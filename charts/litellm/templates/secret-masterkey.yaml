{{- if not .Values.masterkey.existingSecret }}
{{- $secretName := printf "%s-masterkey" (include "litellm.fullname" .) }}
{{- $masterKey := printf "sk-%s" (randAlphaNum 32) }}
{{- if .Values.masterkey.value }}
{{/* An explicit key always wins so it stays reproducible across upgrades. */}}
{{- $masterKey = .Values.masterkey.value }}
{{- else }}
{{- $existingSecret := lookup "v1" "Secret" .Release.Namespace $secretName }}
{{- if $existingSecret }}
  {{- with $existingSecret.data }}
    {{- if hasKey . $.Values.masterkey.secretKey }}
      {{- $masterKey = index . $.Values.masterkey.secretKey | b64dec }}
    {{- end }}
  {{- end }}
{{- end }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "litellm.labels" . | nindent 4 }}
type: Opaque
data:
  {{ .Values.masterkey.secretKey }}: {{ $masterKey | b64enc }}
{{- end }}

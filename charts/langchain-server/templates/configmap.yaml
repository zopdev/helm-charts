apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "langchain-server.fullname" . }}-config
data:
  MODEL_PROVIDER: {{ .Values.env.MODEL_PROVIDER | quote }}
  API_KEYS: {{ .Values.env.API_KEYS | quote }}

image: cassandra:4.1.0
pullPolicy: IfNotPresent

resources:
  requests:
    memory: "2Gi"
    cpu: "1"
  limits:
    memory: "4Gi"
    cpu: "2"

diskSize: 10Gi

config:
  cluster_name: "Cluster"
  num_tokens: 256

cassandradbRootPassword:

services:
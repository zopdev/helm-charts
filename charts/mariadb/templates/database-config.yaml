apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-mariadb-database-config
  namespace: {{ .Release.Namespace }}
data:
  DATABASE_NAME: "{{ .Values.service.name }}" 
  DATABASE_USER: "{{ .Values.service.user }}" 

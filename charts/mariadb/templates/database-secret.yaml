apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-mariadb-database-secret
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  DATABASE_PASSWORD: "{{ .Values.service.password }}"

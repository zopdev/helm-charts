updateStrategy:
  type: RollingUpdate

diskSize: "10Gi"

accessKey: "minioadmin"
secretKey: "minioadmin"

image: "quay.io/minio/minio"

# Resource configuration
resources:
  requests:
    cpu: "100m"
    memory: "100M"
  limits:
    cpu: "1500m"
    memory: "1Gi"
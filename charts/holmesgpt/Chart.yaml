apiVersion: v2
appVersion: "0.39.0"
description: Helm chart for deploying HolmesGPT, an AI agent for investigating production incidents
name: holmesgpt
version: 0.0.1
type: application
# TODO: replace with a zopdev-hosted copy, as every other chart uses.
# `/latest/` is a moving path on the upstream docs site, so a docs
# rebuild can relocate this and break the tile.
icon: "https://holmesgpt.dev/latest/assets/logo.png"
maintainers:
  - name: ZopDev
    url: zop.dev
annotations:
  type: application

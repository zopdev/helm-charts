version: "7.8.0"

zookeeper:
  enabled: true
  url: "" ## If not enabled provide the zookeeper url

resources:
  requests:
    cpu: "500m"
    memory: "500Mi"
  limits:
    cpu: "1000m"
    memory: "1500Mi"

diskSize : 10Gi
{{- range $index, $service := .Values.services }}
{{- $maxLen := 27 }}
{{- $randomSuffix := randAlphaNum 4 | lower }}
{{- $trimmedName := trunc $maxLen $service.name }}
{{- $generatedUsername := printf "%s_%s" $trimmedName $randomSuffix }}
{{- $generatedPassword := printf "%s%s%s" (randAlpha 4) (randNumeric 4) (randAlpha 4 | upper) }}

{{/* Define Secret Name */}}
{{- $secretName := printf "%s-%s-%s-clickhouse-database-secret" $.Release.Name (replace "_" "-" $service.database) $service.name }}

{{/* Retrieve existing secret (if any) so credentials survive upgrades */}}
{{- $existingSecret := lookup "v1" "Secret" $.Release.Namespace $secretName }}

{{- $username := $generatedUsername }}
{{- $password := $generatedPassword }}

{{- if $existingSecret }}
  {{- with $existingSecret.data }}
    {{- if hasKey . "DB_USER" }}
      {{- $username = index . "DB_USER" | b64dec }}
    {{- end }}
    {{- if hasKey . "DB_PASSWORD" }}
      {{- $password = index . "DB_PASSWORD" | b64dec }}
    {{- end }}
  {{- end }}
{{- end }}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $.Release.Name }}-{{ replace "_" "-" $service.database }}-{{ $service.name }}-clickhouse-configmap
  namespace: {{ $.Release.Namespace }}
data:
  DB_DIALECT: "clickhouse"
  DB_USER: "{{ $username }}"
  DB_PORT: "9000"
  DB_NAME: "{{ $service.database }}"
  DB_HOST: "{{ $.Release.Name }}-clickhouse"
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ $.Release.Namespace }}
type: Opaque
data:
  DB_PASSWORD: {{ $password | b64enc }}
  DB_USER: {{ $username | b64enc }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $.Release.Name }}-{{ $service.name }}-init-script
  namespace: {{ $.Release.Namespace }}
data:
  db-init.sql: |
    CREATE DATABASE IF NOT EXISTS `{{ $service.database }}`;

    CREATE USER IF NOT EXISTS '{{ $username }}' IDENTIFIED WITH sha256_password BY '{{ $password }}';

    GRANT SELECT, INSERT, ALTER, CREATE, DROP, TRUNCATE, OPTIMIZE ON `{{ $service.database }}`.* TO '{{ $username }}';
{{- end }}

# values.yaml for OpenTSDB Helm chart
version: 2.2

# Resource configuration
resources:
  requests:
    cpu: "100m"
    memory: "256M"
  limits:
    cpu: "1000m"
    memory: "1Gi"

diskSize: "10Gi"

# Update strategy for the StatefulSet


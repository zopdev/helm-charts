apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}-opentsdb-configmap"
  namespace: {{ .Release.Namespace | quote }}
data:
  OPENTSDB_HOST: "{{ .Values.opentsdb_host}}"
  OPENTSDB_PORT: "{{ .Values.opentsdb_port}}"